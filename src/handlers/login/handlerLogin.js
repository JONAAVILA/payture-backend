import { models } from '../../db.js';
const User = models.User
const SALT_ROUNDS = process.env


const handlerLogin = (password,user)=>{
    try {
        
    } catch (error) {
        
    }
}

export default handlerLogin;